package {{basePackage}}.dtos;

{{#imports}}
import {{.}};
{{/imports}}
import lombok.Data;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
import lombok.Builder;

/**
 * DTO de respuesta para {{className}}.
 * Generado con Project Lombok por UML Studio Architecture Generator.
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class {{className}}ResponseDto {

{{#processedFields}}
    {{#isDateTime}}
    @JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd'T'HH:mm:ss")
    {{/isDateTime}}
    {{#isDate}}
    @JsonFormat(shape = JsonFormat.Shape.STRING, pattern = "yyyy-MM-dd")
    {{/isDate}}
    private {{javaType}} {{name}};
{{/processedFields}}
{{#fkFields}}
    private {{idType}} {{fieldName}}Id;
{{/fkFields}}
{{#hasDiscriminator}}
    private String {{discriminatorFieldName}};
{{/hasDiscriminator}}

    public static {{className}}ResponseDto fromEntity({{className}} entity) {
        if (entity == null) return null;
        return {{className}}ResponseDto.builder()
{{#processedFields}}
            .{{name}}(entity.{{getterName}}())
{{/processedFields}}
{{#fkFields}}
            .{{fieldName}}Id(entity.get{{capFieldName}}() != null ? entity.get{{capFieldName}}().{{targetIdGetterName}}() : null)
{{/fkFields}}
{{#hasDiscriminator}}
            .{{discriminatorFieldName}}(entity.get{{capDiscriminatorFieldName}}() != null ? entity.get{{capDiscriminatorFieldName}}() : "{{discriminatorValue}}")
{{/hasDiscriminator}}
            .build();
    }
}
