package {{basePackage}}.entities;

{{#imports}}
import {{.}};
{{/imports}}
import lombok.Getter;
import lombok.Setter;
import lombok.NoArgsConstructor;
import lombok.AllArgsConstructor;
{{^isInheritanceChild}}
import lombok.Builder;
{{/isInheritanceChild}}

/**
 * Entidad JPA para la tabla '{{tableName}}'.
 * Generada automáticamente con Project Lombok por UML Studio Architecture Generator.
 */
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
{{^isInheritanceChild}}
@Builder
{{/isInheritanceChild}}
@Entity
{{^isInheritanceChild}}
@Table(name = "{{tableName}}")
{{/isInheritanceChild}}
{{#isInheritanceParent}}
@Inheritance(strategy = InheritanceType.SINGLE_TABLE)
@DiscriminatorColumn(name = "{{discriminatorColumnName}}", discriminatorType = DiscriminatorType.STRING, length = 20)
@DiscriminatorValue("{{discriminatorValue}}")
{{/isInheritanceParent}}
{{#isInheritanceChild}}
@DiscriminatorValue("{{discriminatorValue}}")
{{/isInheritanceChild}}
public class {{className}}{{#isInheritanceChild}} extends {{superClassName}}{{/isInheritanceChild}} {

{{#isInheritanceParent}}
    @Column(name = "{{discriminatorColumnName}}", insertable = false, updatable = false)
    private String {{discriminatorFieldName}};

{{/isInheritanceParent}}

{{#processedFields}}
    {{#isId}}
    @Id
    @GeneratedValue(strategy = {{genStrategy}})
    @Column(name = "{{sqlColumnName}}", updatable = false, nullable = false)
    {{/isId}}
    {{^isId}}
    {{#isNotBlank}}
    @NotBlank(message = "El campo '{{name}}' es obligatorio")
    {{/isNotBlank}}
    {{#isNotNull}}
    @NotNull(message = "El campo '{{name}}' no puede ser nulo")
    {{/isNotNull}}
    @Column(name = "{{sqlColumnName}}"{{#isUnique}}, unique = true{{/isUnique}})
    {{/isId}}
    private {{javaType}} {{name}};

{{/processedFields}}
{{#processedRelationships}}
    {{#isManyToOne}}
    @ManyToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "{{joinColumnName}}")
    private {{targetClassName}} {{fieldName}};
    {{/isManyToOne}}
    {{#isOneToMany}}
    @OneToMany(mappedBy = "{{mappedBy}}", cascade = CascadeType.ALL, orphanRemoval = true)
    @JsonIgnore
    @Builder.Default
    private List<{{targetClassName}}> {{fieldName}} = new ArrayList<>();
    {{/isOneToMany}}
    {{#isOneToOne}}
    @OneToOne(fetch = FetchType.LAZY)
    @JoinColumn(name = "{{joinColumnName}}")
    private {{targetClassName}} {{fieldName}};
    {{/isOneToOne}}
    {{#isManyToMany}}
    @ManyToMany
    @JoinTable(
        name = "{{tableName}}_{{fieldName}}",
        joinColumns = @JoinColumn(name = "{{tableName}}_id"),
        inverseJoinColumns = @JoinColumn(name = "{{fieldName}}_id")
    )
    @JsonIgnore
    @Builder.Default
    private List<{{targetClassName}}> {{fieldName}} = new ArrayList<>();
    {{/isManyToMany}}

{{/processedRelationships}}
}
